version: '3.8'

services:
  landslide-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: landslide-ai-service
    ports:
      - "8000:8000"
    environment:
      - PORT=8000
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
